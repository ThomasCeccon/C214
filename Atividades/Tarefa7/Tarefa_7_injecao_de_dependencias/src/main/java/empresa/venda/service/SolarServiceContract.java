package empresa.venda.service;

//define a nome do cliente e a quantidade de placas desejada pelo mesmo
public interface SolarServiceContract {
    void vendaPlacaSolar(String nomeCliente, float quantidade);
}
